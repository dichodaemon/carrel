# No Push to oh-my-pi

**NEVER** run `git push` in the oh-my-pi repository (`/workspace/oh-my-pi`). The user must explicitly authorize every push to oh-my-pi.

This applies to all sessions deployed by carrel.
